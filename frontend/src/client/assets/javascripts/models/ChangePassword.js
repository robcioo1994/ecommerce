export default class ChangePassword{
    constructor(_email, _oldPassword, _newPassword) {
        this.email = _email;
        this.oldPassword = _oldPassword;
        this.newPassword = _newPassword;
    }
}
